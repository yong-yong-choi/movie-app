import React from 'react';
import './Loader.scss';

function Loader() {
    return (
        <div className="loader">
            <h2 className="loader-text">Loading...</h2>
        </div>
    );
}

export default Loader;